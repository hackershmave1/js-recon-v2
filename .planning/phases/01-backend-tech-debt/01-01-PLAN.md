---
phase: 01-backend-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/requirements.txt
  - api/pyproject.toml
autonomous: true
requirements:
  - REQ-02

must_haves:
  truths:
    - "pip install -r api/requirements.txt succeeds without python-jose, passlib, celery, or redis"
    - "No application code imports python-jose, passlib, celery, or redis at runtime"
    - "Celery retention_cleanup task is preserved but clearly documented as requiring optional Celery service"
  artifacts:
    - path: "api/requirements.txt"
      provides: "Trimmed dependency list"
      contains: "alembic"
    - path: "api/pyproject.toml"
      provides: "Primary project definition without dead deps"
  key_links:
    - from: "api/requirements.txt"
      to: "api/pyproject.toml"
      via: "must stay in sync — both files declare the same dependency set"
      pattern: "alembic"
---

<objective>
Remove four dead dependencies — `python-jose`, `passlib`, `celery`, and `redis` — from both `api/requirements.txt` and `api/pyproject.toml`.

Purpose: Eliminate the `python-jose` CVE surface (CVE-2024-33664), remove unused JWT/password-hashing code, and stop requiring a running Redis broker for a service that never dispatches Celery tasks from routes. The Celery task files remain on disk (they handle the real `retention_cleanup` Beat schedule) but are no longer installed in a default `pip install`.

Output: Clean `requirements.txt` and `pyproject.toml` with 4 fewer packages. No runtime behaviour changes.
</objective>

<execution_context>
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/phases/01-backend-tech-debt/01-01-PLAN.md
</execution_context>

<context>
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/PROJECT.md
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/ROADMAP.md
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no runtime imports of dead packages before removal</name>
  <files>api/app/ (read-only scan)</files>
  <read_first>
    - api/requirements.txt
    - api/pyproject.toml
    - api/app/tasks/celery_app.py
    - api/app/tasks/retention_cleanup.py
    - api/app/tasks/enhanced_processing.py
    - api/app/tasks/process_file.py
  </read_first>
  <action>
Before touching any file, confirm the following import patterns do NOT appear anywhere under api/app/ (excluding tasks/):

1. `from jose` or `import jose` — python-jose, JWT library, never wired to routes
2. `from passlib` or `import passlib` — password hashing, never wired to routes
3. `from celery` or `import celery` — outside api/app/tasks/ only (tasks/ is allowed to import celery; routes/ must not)
4. `import redis` — outside api/app/tasks/ only

Confirm celery IS used in api/app/tasks/:
- api/app/tasks/celery_app.py imports `from celery import Celery`
- api/app/tasks/retention_cleanup.py uses `@celery_app.task`
- api/app/tasks/enhanced_processing.py uses `@celery_app.task`

Document findings inline as comments in the plan execution notes. If any route file under api/app/api/ imports celery, python-jose, or passlib, STOP and report — do NOT remove those packages.

Verification: This task produces no file changes; it is a confirmation gate.
  </action>
  <verify>
    <automated>grep -rn "from jose\|import jose\|from passlib\|import passlib\|from celery\|import celery\|import redis" /home/shay/Projects/JavascriptRecon/js-security-extractor/api/app/api/ 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    The grep above returns no matches (zero lines) under api/app/api/. Exit code 1 from grep (no matches) is expected and correct.
  </acceptance_criteria>
  <done>Zero imports of jose/passlib/celery/redis found under api/app/api/. Safe to remove.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead packages from requirements.txt and pyproject.toml</name>
  <files>api/requirements.txt, api/pyproject.toml</files>
  <read_first>
    - api/requirements.txt
    - api/pyproject.toml
  </read_first>
  <action>
Edit BOTH files to remove exactly these four packages. Both files must be edited — they declare the same dependency set.

Packages to remove:
- `celery==5.3.4` (and any `celery` line variant)
- `redis==5.0.1` (and any `redis` line variant)
- `python-jose[cryptography]==3.3.0` (and any `python-jose` line variant)
- `passlib[bcrypt]==1.7.4` (and any `passlib` line variant)

All other packages must remain exactly as-is. Do not change version pins on any other package.

After editing, add a comment block at the top of api/requirements.txt (above the first package line) explaining the removed packages:

```
# Removed dependencies (2026-04-19):
#   python-jose[cryptography] — JWT library, no active auth routes; CVE-2024-33664
#   passlib[bcrypt]           — Password hashing, no active auth routes
#   celery                    — Tasks in api/app/tasks/ are retained but require
#                               celery installed separately when running the Beat worker
#   redis                     — Only needed when running Celery Beat worker
```

The `api/app/tasks/` directory and all Celery task files remain on disk — they are NOT deleted. The docker-compose.yml services (celery_worker, celery_beat) remain — they are NOT modified. Only requirements files change.
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
txt = open('/home/shay/Projects/JavascriptRecon/js-security-extractor/api/requirements.txt').read()
toml = open('/home/shay/Projects/JavascriptRecon/js-security-extractor/api/pyproject.toml').read()
dead = ['python-jose', 'passlib', 'celery', 'redis']
errors = []
for pkg in dead:
    if pkg.lower() in txt.lower():
        errors.append(f'requirements.txt still contains {pkg}')
    if pkg.lower() in toml.lower():
        errors.append(f'pyproject.toml still contains {pkg}')
kept = ['fastapi', 'sqlalchemy', 'alembic', 'pydantic', 'httpx', 'aiofiles', 'uvicorn']
for pkg in kept:
    if pkg not in txt:
        errors.append(f'requirements.txt missing expected package: {pkg}')
    if pkg not in toml:
        errors.append(f'pyproject.toml missing expected package: {pkg}')
if errors:
    print('FAIL:', errors)
    sys.exit(1)
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "python-jose\|passlib\|^celery\|^redis" api/requirements.txt` returns no matches
    - `grep -i "python-jose\|passlib\|celery\|redis" api/pyproject.toml` returns no matches (within the dependencies array)
    - `api/requirements.txt` still contains: fastapi, sqlalchemy, alembic, pydantic, httpx, aiofiles, uvicorn, psycopg2-binary, python-multipart, jinja2, pytest
    - `api/pyproject.toml` still contains the same kept packages in the `dependencies` array
    - Comment block explaining removals is present at top of api/requirements.txt
    - api/app/tasks/ directory and all .py files inside remain untouched
  </acceptance_criteria>
  <done>Both requirements files cleaned; 4 dead packages removed; comment added; task files untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirements.txt → pip install | Package removal must not break runtime imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | requirements.txt | mitigate | Verify all kept packages still present after edit via automated check in Task 2 verify step |
| T-01-01-02 | Information Disclosure | python-jose CVE-2024-33664 | mitigate | Removal of python-jose eliminates JWT algorithm confusion attack surface |
</threat_model>

<verification>
```bash
cd /home/shay/Projects/JavascriptRecon/js-security-extractor
grep -c "python-jose\|passlib\|^celery\|^redis" api/requirements.txt
# Expected: 0

grep "alembic\|fastapi\|sqlalchemy" api/requirements.txt
# Expected: all three present with version pins

ls api/app/tasks/
# Expected: celery_app.py, enhanced_processing.py, process_file.py, retention_cleanup.py all present
```
</verification>

<success_criteria>
- `python-jose`, `passlib`, `celery`, `redis` absent from both requirements.txt and pyproject.toml
- All 11 other packages remain with original version pins
- api/app/tasks/ directory unmodified (Celery task files intact for when Beat worker is deployed)
- Comment block at top of requirements.txt explains why each package was removed
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-tech-debt/01-01-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
