---
phase: 02-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - api/app/static/dashboard.js
autonomous: true
requirements:
  - REQ-07

must_haves:
  truths:
    - "getEmptyState() accepts three parameters: title, body, icon"
    - "getEmptyState() emits a .empty-state-title <p> and a .empty-state-body <p>"
    - "getEmptyState() emits the icon with classes: fas fa-{icon} mb-2 fa-2x text-muted"
    - "All 6 callsites pass distinct title + body + icon arguments"
    - "No callsite passes only 2 arguments to getEmptyState"
  artifacts:
    - path: "api/app/static/dashboard.js"
      provides: "Updated getEmptyState function and all callsites"
      contains: "getEmptyState(title, body, icon)"
  key_links:
    - from: "getEmptyState() in dashboard.js"
      to: ".empty-state-title / .empty-state-body CSS classes in dashboard.css"
      via: "innerHTML assignment on container element"
      pattern: "empty-state-title|empty-state-body"
---

<objective>
Rewrite `getEmptyState()` in dashboard.js to emit the two-level markup (.empty-state-title + .empty-state-body) that the CSS was designed to render, and supply contextual body copy at every callsite.

Purpose: Every empty result panel currently falls back to a flat unstyled `<p>` tag, losing the semibold title + muted subtext layout already defined in dashboard.css:538-539. Users see "No endpoints found" with no guidance — rewriting restores the intended visual design and adds actionable subtext.

Output: Modified dashboard.js only.
</objective>

<execution_context>
@/home/shay/.claude/get-shit-done/workflows/execute-plan.md
@/home/shay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/ROADMAP.md
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/STATE.md
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/phases/02-ui-polish/02-02-SUMMARY.md

<interfaces>
<!-- Current getEmptyState definition — dashboard.js lines 3592-3599 -->
Current signature:
  getEmptyState(message, icon)

Current body:
  return `
    <div class="empty-state">
        <i class="fas fa-${icon}"></i>
        <p>${message}</p>
    </div>
  `;

Target signature:
  getEmptyState(title, body, icon)

Target body (exact markup to produce):
  return `
    <div class="empty-state">
        <i class="fas fa-${icon} mb-2 fa-2x text-muted"></i>
        <p class="empty-state-title">${title}</p>
        <p class="empty-state-body">${body}</p>
    </div>
  `;

<!-- All 6 callsites with current args → replacement args -->
<!-- Verified from grep output above -->

1. Line 1744: this.getEmptyState('No endpoints found', 'globe')
   → this.getEmptyState('No endpoints found', 'Run an analysis to populate endpoint results.', 'globe')

2. Line 1788: this.getEmptyState('No secrets found', 'key')
   → this.getEmptyState('No secrets found', 'Run an analysis to scan for secrets and API keys.', 'key')

3. Line 1839: this.getEmptyState('No dependencies found', 'sitemap')
   → this.getEmptyState('No dependencies found', 'Run an analysis to detect JS dependencies.', 'sitemap')

4. Line 1892: this.getEmptyState('No source maps found', 'map')
   → this.getEmptyState('No source maps found', 'Source maps are detected automatically during analysis.', 'map')

5. Line 2165: this.getEmptyState(emptyText, 'folder')
   → this.getEmptyState(emptyText, 'Select a session above to browse its captured files.', 'folder')

6. Line 2765: this.getEmptyState(emptyText, 'history')
   → this.getEmptyState(emptyText, 'Create a session to start capturing and analyzing JavaScript.', 'history')

Note: Lines 1744, 1788, 1839, 1892 have static first arguments; lines 2165 and 2765 use a dynamic `emptyText` variable as the first argument. In both dynamic cases the variable already contains a complete sentence — pass it as `title` and add the new fixed `body` string as the second argument.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite getEmptyState function signature and body</name>
  <read_first>api/app/static/dashboard.js</read_first>
  <files>api/app/static/dashboard.js</files>
  <action>
Find the `getEmptyState` method definition in dashboard.js (currently at line ~3592). Replace the entire method (the line `getEmptyState(message, icon) {` through the closing `}`) with the following:

```javascript
    getEmptyState(title, body, icon) {
        return `
            <div class="empty-state">
                <i class="fas fa-${icon} mb-2 fa-2x text-muted"></i>
                <p class="empty-state-title">${title}</p>
                <p class="empty-state-body">${body}</p>
            </div>
        `;
    }
```

Preserve the surrounding indentation (4 spaces, consistent with rest of the class methods). Do NOT change any other method.
  </action>
  <verify>
Run: `grep -A 8 "getEmptyState(title" api/app/static/dashboard.js`
Expected output:
  getEmptyState(title, body, icon) {
      return `
          <div class="empty-state">
              <i class="fas fa-${icon} mb-2 fa-2x text-muted"></i>
              <p class="empty-state-title">${title}</p>
              <p class="empty-state-body">${body}</p>
          </div>
      `;
  }
  </verify>
  <acceptance_criteria>
  - `grep "getEmptyState(title, body, icon)" api/app/static/dashboard.js` returns exactly 1 match (the definition)
  - `grep "empty-state-title" api/app/static/dashboard.js` returns at least 1 match
  - `grep "empty-state-body" api/app/static/dashboard.js` returns at least 1 match
  - `grep "fa-2x text-muted" api/app/static/dashboard.js` returns 1 match (inside getEmptyState)
  - `grep "getEmptyState(message" api/app/static/dashboard.js` returns no output (old signature gone)
  </acceptance_criteria>
  <done>getEmptyState() accepts 3 params and emits .empty-state-title + .empty-state-body markup.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 6 callsites with 3-argument form and contextual body copy</name>
  <read_first>api/app/static/dashboard.js</read_first>
  <files>api/app/static/dashboard.js</files>
  <action>
Find and replace each of the 6 getEmptyState callsites in dashboard.js. Make the exact replacements listed below. Only change the argument list — do not alter surrounding indentation, variable assignments, or logic.

Replacement 1 (around line 1744):
  OLD: this.getEmptyState('No endpoints found', 'globe')
  NEW: this.getEmptyState('No endpoints found', 'Run an analysis to populate endpoint results.', 'globe')

Replacement 2 (around line 1788):
  OLD: this.getEmptyState('No secrets found', 'key')
  NEW: this.getEmptyState('No secrets found', 'Run an analysis to scan for secrets and API keys.', 'key')

Replacement 3 (around line 1839):
  OLD: this.getEmptyState('No dependencies found', 'sitemap')
  NEW: this.getEmptyState('No dependencies found', 'Run an analysis to detect JS dependencies.', 'sitemap')

Replacement 4 (around line 1892):
  OLD: this.getEmptyState('No source maps found', 'map')
  NEW: this.getEmptyState('No source maps found', 'Source maps are detected automatically during analysis.', 'map')

Replacement 5 (around line 2165):
  OLD: this.getEmptyState(emptyText, 'folder')
  NEW: this.getEmptyState(emptyText, 'Select a session above to browse its captured files.', 'folder')

Replacement 6 (around line 2765):
  OLD: this.getEmptyState(emptyText, 'history')
  NEW: this.getEmptyState(emptyText, 'Create a session to start capturing and analyzing JavaScript.', 'history')

After replacing all 6, verify no 2-argument form of getEmptyState remains.
  </action>
  <verify>
Run: `grep -n "getEmptyState" api/app/static/dashboard.js`
Expected output: exactly 7 lines (1 definition + 6 callsites).
Every callsite line must contain 3 arguments (two commas between the parentheses).

Run: `grep "getEmptyState('[^']*', '[^']\|getEmptyState(emptyText, '" api/app/static/dashboard.js`
→ Should return only 2-argument (old style) lines — expect ZERO matches.
  </verify>
  <acceptance_criteria>
  - `grep -c "getEmptyState" api/app/static/dashboard.js` returns 7
  - `grep "getEmptyState('No endpoints found', 'globe')" api/app/static/dashboard.js` returns no output (old 2-arg form gone)
  - `grep "getEmptyState('No secrets found', 'key')" api/app/static/dashboard.js` returns no output
  - `grep "getEmptyState('No dependencies found', 'sitemap')" api/app/static/dashboard.js` returns no output
  - `grep "getEmptyState('No source maps found', 'map')" api/app/static/dashboard.js` returns no output
  - `grep "getEmptyState(emptyText, 'folder')" api/app/static/dashboard.js` returns no output (old 2-arg form)
  - `grep "getEmptyState(emptyText, 'history')" api/app/static/dashboard.js` returns no output (old 2-arg form)
  - `grep "Run an analysis to populate" api/app/static/dashboard.js` returns 1 match
  - `grep "Run an analysis to scan" api/app/static/dashboard.js` returns 1 match
  - `grep "Source maps are detected" api/app/static/dashboard.js` returns 1 match
  - `grep "Create a session to start" api/app/static/dashboard.js` returns 1 match
  </acceptance_criteria>
  <done>All 6 callsites pass title, body, and icon; each empty-state panel now has actionable subtext matching its context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response data → innerHTML | Empty state HTML is static template strings; no user-controlled data injected into the new markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | getEmptyState body copy | accept | Body strings are hardcoded developer copy, not dynamic data; no XSS surface added |
</threat_model>

<verification>
End-to-end check after both tasks:
1. `grep -c "getEmptyState" api/app/static/dashboard.js` → 7
2. `grep "getEmptyState(title, body, icon)" api/app/static/dashboard.js` → 1 match (definition)
3. `grep "empty-state-title\|empty-state-body" api/app/static/dashboard.js` → 2 matches (in the function body)
4. `grep "getEmptyState.*'globe'\|getEmptyState.*'key'\|getEmptyState.*'sitemap'\|getEmptyState.*'map'\|getEmptyState.*'folder'\|getEmptyState.*'history'" api/app/static/dashboard.js` → 6 lines, each with 3 arguments
</verification>

<success_criteria>
- getEmptyState(title, body, icon) signature in place
- Emits `<p class="empty-state-title">` and `<p class="empty-state-body">` elements
- Icon uses `fa-2x text-muted` classes to match CSS intent
- All 6 callsites provide distinct, contextual body copy
- No 2-argument callsites remain
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-polish/02-03-SUMMARY.md`
</output>
