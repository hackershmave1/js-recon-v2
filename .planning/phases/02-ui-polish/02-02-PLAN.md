---
phase: 02-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/static/dashboard.css
autonomous: true
requirements:
  - REQ-06

must_haves:
  truths:
    - "dashboard.css :root defines exactly 5 named font-size tokens: --text-xs through --text-lg"
    - "All 14+ individual font-size declarations in dashboard.css use one of the 5 named tokens"
    - "No raw rem font-size values remain outside the :root block (0.775rem, 0.82rem, 0.85rem, 0.9rem, 1.05rem are fully eliminated)"
    - "body font-size: 15px stays as-is (base document size, not part of the rem scale)"
  artifacts:
    - path: "api/app/static/dashboard.css"
      provides: "5-stop typography scale via CSS custom properties"
      contains: "--text-xs:"
  key_links:
    - from: "dashboard.css :root --text-xs through --text-lg"
      to: "all font-size declarations in the file"
      via: "var(--text-*) references"
      pattern: "var\\(--text-"
---

<objective>
Collapse dashboard.css's 14-stop font-size chaos into a clean 5-stop CSS custom property scale, eliminating unmaintainable near-identical sizes like 0.8rem, 0.8125rem, 0.82rem, and 0.85rem.

Purpose: The proliferation of one-off sizes makes future type maintenance brittle and causes subtle unintentional variation. Five named stops are sufficient for all application UI contexts.

Output: Modified dashboard.css only — no HTML or JS changes.
</objective>

<execution_context>
@/home/shay/.claude/get-shit-done/workflows/execute-plan.md
@/home/shay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/ROADMAP.md
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/STATE.md

<interfaces>
<!-- Complete inventory of every font-size declaration in dashboard.css, with target token mapping -->
<!-- Extracted from dashboard.css — read the file before editing -->

SCALE DEFINITION to add to :root (insert after the existing --font-mono line, before the Animation block):
  --text-xs:   0.6875rem;   /* 11px — badges, table headers, status bar, stat labels, nav section labels */
  --text-sm:   0.75rem;     /* 12px — file URLs, filter labels, api-status-dot, titlebar-btn */
  --text-base: 0.8125rem;   /* 13px — body small text: card headers h5/h6, btn-sm, code/pre, context cards, dependency tree */
  --text-md:   0.875rem;    /* 14px — nav items, form labels, check labels, tab links, export options, sourcemap file headers, file-table rows */
  --text-lg:   0.9375rem;   /* 15px — btn-lg, empty-state-title, titlebar-brand */

MAPPING TABLE (every font-size declaration → token):
  Line  89: body font-size: 15px             → LEAVE AS-IS (document base, px not rem)
  Line 143: nav-tabs .nav-link 0.875rem       → var(--text-md)
  Line 158: .form-check-label 0.875rem        → var(--text-md)
  Line 186: .card-header h5/h6 0.8125rem      → var(--text-base)
  Line 199: #js-content 0.8125rem             → var(--text-base)
  Line 225: .btn 0.875rem                     → var(--text-md)
  Line 259: .btn-lg 0.9375rem                 → var(--text-lg)
  Line 260: .btn-sm 0.8125rem                 → var(--text-base)
  Line 282: .list-filter-bar .form-label 0.75rem → var(--text-sm)
  Line 307: .result-url 0.8rem                → var(--text-base)   [rounds 0.8 → 0.8125]
  Line 319: .result-context 0.8rem            → var(--text-base)   [rounds 0.8 → 0.8125]
  Line 327: .confidence-badge 0.6875rem       → var(--text-xs)
  Line 335: .secret-value 0.8125rem           → var(--text-base)
  Line 372: .dependency-tree 0.8125rem        → var(--text-base)
  Line 382: .sourcemap-preview 0.8rem         → var(--text-base)   [rounds 0.8 → 0.8125]
  Line 389: pre 0.8125rem                     → var(--text-base)
  Line 438: @media .stat-num 1.25rem          → LEAVE AS-IS (responsive override for stat number)
  Line 452: .file-table 0.875rem              → var(--text-md)
  Line 454: .file-table th 0.6875rem          → var(--text-xs)
  Line 468: .file-url (table) 0.75rem         → var(--text-sm)
  Line 476: .file-size 0.75rem                → var(--text-sm)
  Line 494: .badge 0.6875rem                  → var(--text-xs)
  Line 523: .export-option 0.875rem           → var(--text-md)
  Line 537: .empty-state i 2.5rem             → LEAVE AS-IS (icon-size, not typography)
  Line 538: .empty-state-title 0.9375rem      → var(--text-lg)
  Line 539: .empty-state-body 0.8125rem       → var(--text-base)
  Line 558: .app-statusbar 0.6875rem          → var(--text-xs)
  Line 630: .session-name-edit-trigger 0.8rem → var(--text-base)   [rounds 0.8 → 0.8125]
  Line 648: #sessionAnalyzeConfigModal .modal-title 1.05rem → var(--text-lg)   [rounds 1.05 → 0.9375; visually near-identical]
  Line 652: #fileAnalyzeConfigModal .modal-title 1.05rem   → var(--text-lg)
  Line 663: .failure-panel-title 0.85rem      → var(--text-md)     [rounds 0.85 → 0.875]
  Line 664: .failure-panel-details 0.82rem    → var(--text-base)   [rounds 0.82 → 0.8125]
  Line 665: .failure-panel-guidance 0.8rem    → var(--text-base)
  Line 669: .analysis-context-card-header 0.8125rem → var(--text-base)
  Line 671: .analysis-context-label 0.8125rem → var(--text-base)
  Line 672: .analysis-context-value 0.8125rem → var(--text-base)
  Line 678: form-label in modals 0.85rem      → var(--text-md)     [rounds 0.85 → 0.875]
  Line 680: form-check-label in modals 0.9rem → var(--text-lg)     [rounds 0.9 → 0.9375]
  Line 683: .session-analyze-help 0.85rem     → var(--text-md)
  Line 693: .pipeline 0.75rem                 → var(--text-sm)
  Line 718: .sourcemap-validation-title 0.875rem  → var(--text-md)
  Line 720: .sourcemap-validation-reasons 0.8rem  → var(--text-base)
  Line 721: .sourcemap-lifecycle-line 0.775rem    → var(--text-sm)   [rounds 0.775 → 0.75]
  Line 726: .sourcemap-file-header 0.875rem    → var(--text-md)
  Line 750: .titlebar-brand 0.9375rem          → var(--text-lg)
  Line 761: .api-status-dot 0.75rem            → var(--text-sm)
  Line 793: .titlebar-btn 0.75rem              → var(--text-sm)
  Line 827: .nav-section-label 0.6875rem       → var(--text-xs)
  Line 842: .nav-item 0.875rem                 → var(--text-md)
  Line 876: .stat-num 1.5rem                   → LEAVE AS-IS (display number, intentionally large)
  Line 884: .stat-lbl 0.6875rem                → var(--text-xs)

TOKENS TO LEAVE UNCHANGED (not part of the typography scale):
  - body font-size: 15px (document root)
  - .stat-num 1.5rem and @media override 1.25rem (stat display numbers)
  - .empty-state i 2.5rem (icon display size)
  - code font-size: 0.875em (em unit, relative to parent — do NOT change to rem token)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5-stop scale tokens to :root</name>
  <read_first>api/app/static/dashboard.css</read_first>
  <files>api/app/static/dashboard.css</files>
  <action>
In dashboard.css, find the Typography block inside :root (currently contains `--font-sans` and `--font-mono` lines, around lines 47-48). Insert the following 5 lines directly after the `--font-mono` line and before the Animation comment block:

  /* Typography scale */
  --text-xs:   0.6875rem;
  --text-sm:   0.75rem;
  --text-base: 0.8125rem;
  --text-md:   0.875rem;
  --text-lg:   0.9375rem;

Do NOT change any existing lines in :root. Only add these 5 new custom property definitions.
  </action>
  <verify>
Run: `grep -n "\-\-text-" api/app/static/dashboard.css | head -10`
Expected output — exactly these 5 lines (in any order within :root):
  --text-xs:   0.6875rem;
  --text-sm:   0.75rem;
  --text-base: 0.8125rem;
  --text-md:   0.875rem;
  --text-lg:   0.9375rem;
  </verify>
  <acceptance_criteria>
  - `grep -c "\-\-text-" api/app/static/dashboard.css` returns 5
  - `grep "\-\-text-xs:" api/app/static/dashboard.css` contains `0.6875rem`
  - `grep "\-\-text-sm:" api/app/static/dashboard.css` contains `0.75rem`
  - `grep "\-\-text-base:" api/app/static/dashboard.css` contains `0.8125rem`
  - `grep "\-\-text-md:" api/app/static/dashboard.css` contains `0.875rem`
  - `grep "\-\-text-lg:" api/app/static/dashboard.css` contains `0.9375rem`
  </acceptance_criteria>
  <done>Five named typography tokens are present in :root and available for all subsequent declarations to reference.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all one-off font-size values with scale tokens</name>
  <read_first>api/app/static/dashboard.css</read_first>
  <files>api/app/static/dashboard.css</files>
  <action>
Replace every `font-size:` declaration in dashboard.css according to the mapping table in the interfaces block above. Proceed line by line in order. For each replacement:

- Use the exact `var(--text-*)` reference shown in the mapping table
- Do NOT alter anything else on the line (surrounding properties, selectors, comments)
- Lines explicitly marked "LEAVE AS-IS" must not be touched:
  - body `font-size: 15px`
  - `.stat-num` `font-size: 1.5rem`
  - `@media` override `.stat-num { font-size: 1.25rem; }`
  - `.empty-state i` `font-size: 2.5rem`
  - `code` `font-size: 0.875em` (em unit)

After all replacements, no raw rem font-size values should remain in the file body (outside of :root token definitions and the LEAVE AS-IS exceptions above).

Specifically ensure these values are fully eliminated from non-:root selectors:
  0.775rem, 0.8rem, 0.82rem, 0.85rem, 0.8125rem (replaced by var(--text-base)),
  0.875rem (replaced by var(--text-md)),
  0.9rem, 1.05rem,
  0.75rem (replaced by var(--text-sm)),
  0.6875rem (replaced by var(--text-xs)),
  0.9375rem (replaced by var(--text-lg))
  </action>
  <verify>
Run the following checks sequentially:

1. `grep -n "font-size: 0\." api/app/static/dashboard.css | grep -v "\-\-text-\|1\.5rem\|1\.25rem\|2\.5rem\|0\.875em"`
   → Should return ZERO lines (all raw rem sizes eliminated from selectors)

2. `grep -c "var(--text-" api/app/static/dashboard.css`
   → Should return at least 35 (one for each replaced declaration)

3. `grep "font-size: 0\.775rem\|font-size: 0\.82rem\|font-size: 0\.85rem\|font-size: 0\.9rem\|font-size: 1\.05rem" api/app/static/dashboard.css`
   → Should return ZERO lines
  </verify>
  <acceptance_criteria>
  - `grep -c "var(--text-" api/app/static/dashboard.css` returns >= 35
  - `grep "font-size: 0\.775rem" api/app/static/dashboard.css` returns no output
  - `grep "font-size: 0\.82rem" api/app/static/dashboard.css` returns no output
  - `grep "font-size: 0\.85rem" api/app/static/dashboard.css` returns no output
  - `grep "font-size: 0\.9rem" api/app/static/dashboard.css` returns no output
  - `grep "font-size: 1\.05rem" api/app/static/dashboard.css` returns no output
  - `grep "font-size: 15px" api/app/static/dashboard.css` returns 1 match (body, unchanged)
  - `grep "font-size: 1\.5rem" api/app/static/dashboard.css` returns 1 match (.stat-num, unchanged)
  - `grep "font-size: 2\.5rem" api/app/static/dashboard.css` returns 1 match (.empty-state i, unchanged)
  </acceptance_criteria>
  <done>All 14+ one-off font-size values replaced with 5-stop var(--text-*) references; the complete list of eliminated values (0.775rem, 0.82rem, 0.85rem, 0.9rem, 1.05rem, and collapsed siblings) no longer appears in any selector rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS property values → rendered text | Client-side only; no security boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | CSS custom property definitions | accept | Typography token changes are client-side CSS with no security impact |
</threat_model>

<verification>
Full file verification after both tasks:
1. `grep -n "\-\-text-" api/app/static/dashboard.css | head -10` → exactly 5 token definitions in :root
2. `grep -c "var(--text-" api/app/static/dashboard.css` → >= 35 usages
3. `grep "font-size: 0\.775\|0\.82\|0\.85\|0\.9rem\|1\.05rem" api/app/static/dashboard.css` → zero matches
4. Spot-check: `grep -n "failure-panel-title\|failure-panel-details\|modal-title" api/app/static/dashboard.css` → all three should show `var(--text-*)` values
</verification>

<success_criteria>
- dashboard.css :root contains exactly the 5 tokens: --text-xs (0.6875rem), --text-sm (0.75rem), --text-base (0.8125rem), --text-md (0.875rem), --text-lg (0.9375rem)
- All one-off font-size values in selectors replaced with var(--text-*) references
- The 5 "one-off" offenders from the audit (0.775rem, 0.82rem, 0.85rem, 0.9rem, 1.05rem) are gone
- body:15px, .stat-num:1.5rem, .empty-state i:2.5rem, code:0.875em are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-polish/02-02-SUMMARY.md`
</output>
