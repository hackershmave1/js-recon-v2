---
phase: 02-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/static/dashboard.css
  - chrome-extension/popup.html
  - chrome-extension/options.html
autonomous: true
requirements:
  - REQ-05

must_haves:
  truths:
    - "dashboard.css :root block defines --accent: #356AE6 and --accent-hover: #2B5DCC (already present; verify no second definition overrides them)"
    - "popup.html :root block defines --accent: #356AE6 (replacing #0a84ff)"
    - "popup.html :root block defines --accent-hover: #2B5DCC (new token)"
    - "options.html :root block defines --accent: #356AE6 (replacing #0a84ff)"
    - "options.html :root block defines --accent-hover: #2B5DCC (new token)"
    - "popup.html .status-indicator default background uses var(--muted) instead of hardcoded #cfd3d9"
    - "popup.html .btn-primary uses var(--accent) (replacing var(--accent-2) / #111315)"
    - "options.html .btn-primary uses var(--accent)"
    - "options.html #status.success color and border-color use token variables, not hardcoded #1d5b35/#7a1f1f"
  artifacts:
    - path: "api/app/static/dashboard.css"
      provides: "Single source of truth for --accent token (already #356AE6)"
      contains: "--accent:         #356AE6"
    - path: "chrome-extension/popup.html"
      provides: "Extension popup with unified accent token"
      contains: "--accent: #356AE6"
    - path: "chrome-extension/options.html"
      provides: "Extension settings page with unified accent token"
      contains: "--accent: #356AE6"
  key_links:
    - from: "popup.html :root --accent"
      to: ".stat-value, .btn-secondary, .file-url, accent-color"
      via: "var(--accent) CSS custom property"
      pattern: "var\\(--accent\\)"
    - from: "options.html :root --accent"
      to: ".btn-primary, accent-color"
      via: "var(--accent) CSS custom property"
      pattern: "var\\(--accent\\)"
---

<objective>
Unify the primary accent color across the dashboard and Chrome extension so both surfaces use the same cornflower-blue hue family (#356AE6).

Purpose: A security tool's two surfaces (web dashboard + extension popup) currently show two distinct brand blues — #356AE6 (warm cornflower) vs #0a84ff (iOS system blue). This erodes brand trust and is the highest-priority color finding from the UI audit.

Output: popup.html and options.html with updated :root tokens; no new files created.
</objective>

<execution_context>
@/home/shay/.claude/get-shit-done/workflows/execute-plan.md
@/home/shay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/PROJECT.md
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/ROADMAP.md
@/home/shay/Projects/JavascriptRecon/js-security-extractor/.planning/STATE.md

<interfaces>
<!-- Extracted token values in effect before this plan executes -->

From api/app/static/dashboard.css :root (lines 24-29) — REFERENCE ONLY, do not change:
  --accent:         #356AE6;   /* target hue — extension must match this */
  --accent-glow:    rgba(53,106,230,0.18);
  --accent-subtle:  rgba(53,106,230,0.10);
  --accent-hover:   #2B5DCC;
  --accent-border:  rgba(53,106,230,0.28);

From chrome-extension/popup.html :root (lines 7-20) — CURRENT VALUES TO REPLACE:
  --accent:   #0a84ff;   /* → change to #356AE6 */
  --accent-2: #111315;   /* near-black — keep as token but stop using it for .btn-primary */

From chrome-extension/options.html :root (lines 7-20) — CURRENT VALUES TO REPLACE:
  --accent:   #0a84ff;   /* → change to #356AE6 */
  --accent-2: #111315;   /* keep as token */

From popup.html HTML (line 79):
  .status-indicator { background: #cfd3d9; }   /* → change to var(--muted) */

From popup.html CSS (lines 161-174):
  .btn-primary  { background: var(--accent-2); }   /* → change to var(--accent) */
  .btn-secondary { background: var(--accent); }    /* currently used for Export Files — keep as is */

From options.html CSS (lines 105-107):
  .btn-primary { background: var(--accent-2); }   /* → change to var(--accent) */

From options.html CSS (lines 136-144) — hardcoded status colors to tokenize:
  #status.success { color: #1d5b35; border: 1px solid #b9e4c8; background: #ecf9f1; }
  #status.error   { color: #7a1f1f; border: 1px solid #f3c3c3; background: #fdecec; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update popup.html accent token and dependent color values</name>
  <read_first>chrome-extension/popup.html</read_first>
  <files>chrome-extension/popup.html</files>
  <action>
Make the following targeted edits to chrome-extension/popup.html:

1. In the `:root` block (around line 14), change:
   `--accent: #0a84ff;`
   to:
   `--accent: #356AE6;`
   Also add below the existing `--accent` line (if not present):
   `--accent-hover: #2B5DCC;`

2. In the `.status-indicator` rule (around line 79), change:
   `background: #cfd3d9;`
   to:
   `background: var(--muted);`

3. In the `.btn-primary` rule (around line 161-163), change:
   `background: var(--accent-2);`
   to:
   `background: var(--accent);`
   Also add below it:
   `border-color: var(--accent);`

   NOTE: `.btn-secondary` currently uses `var(--accent)` for Export Files — that is correct and intentional, leave it unchanged. Only change `.btn-primary`.

4. Do NOT change `--accent-2: #111315` in the :root block — keep it as a fallback token. Do NOT change `.btn-danger`, `.btn-light`, or any other rule.
  </action>
  <verify>
Run: `grep -n "accent" chrome-extension/popup.html`
Expected output must include:
  - `--accent: #356AE6;`
  - `--accent-hover: #2B5DCC;`
  - `background: var(--muted);` (on the .status-indicator rule)
  - `.btn-primary` rule containing `background: var(--accent);`
Must NOT contain `#0a84ff` anywhere.
Must NOT contain `background: #cfd3d9`.
  </verify>
  <acceptance_criteria>
  - `grep "#0a84ff" chrome-extension/popup.html` returns no output
  - `grep "#cfd3d9" chrome-extension/popup.html` returns no output
  - `grep "\-\-accent: #356AE6" chrome-extension/popup.html` returns exactly 1 match
  - `grep "\-\-accent-hover: #2B5DCC" chrome-extension/popup.html` returns exactly 1 match
  - `grep "background: var(--muted)" chrome-extension/popup.html` returns 1 match on the .status-indicator rule
  - `.btn-primary` block in popup.html contains `background: var(--accent)` (not `var(--accent-2)`)
  </acceptance_criteria>
  <done>popup.html uses #356AE6 as its accent; Start Capture button renders in cornflower blue matching the dashboard; status indicator default uses the muted token.</done>
</task>

<task type="auto">
  <name>Task 2: Update options.html accent token and status color tokens</name>
  <read_first>chrome-extension/options.html</read_first>
  <files>chrome-extension/options.html</files>
  <action>
Make the following targeted edits to chrome-extension/options.html:

1. In the `:root` block (around line 14), change:
   `--accent: #0a84ff;`
   to:
   `--accent: #356AE6;`
   Also add below the existing `--accent` line (if not present):
   `--accent-hover: #2B5DCC;`

2. In the `.btn-primary` rule (around line 105-107), change:
   `background: var(--accent-2);`
   to:
   `background: var(--accent);`
   Also add:
   `border-color: var(--accent);`

3. Add `--success-text: #1d5b35;` and `--danger-text: #7a1f1f;` to the `:root` block. Then update the two hardcoded `#status` color rules (around lines 136-144):

   Current:
   ```css
   #status.success {
     color: #1d5b35;
     border: 1px solid #b9e4c8;
     background: #ecf9f1;
   }
   #status.error {
     color: #7a1f1f;
     border: 1px solid #f3c3c3;
     background: #fdecec;
   }
   ```

   Replace with:
   ```css
   #status.success {
     color: var(--success-text);
     border: 1px solid #b9e4c8;
     background: #ecf9f1;
   }
   #status.error {
     color: var(--danger-text);
     border: 1px solid #f3c3c3;
     background: #fdecec;
   }
   ```

   (The background/border values for status boxes are aesthetic tints, not brand tokens; leave them as literal values.)

4. Do NOT change any checkbox `accent-color` properties, form inputs, or any other rules.
  </action>
  <verify>
Run: `grep -n "accent\|1d5b35\|7a1f1f\|0a84ff" chrome-extension/options.html`
Expected:
  - `--accent: #356AE6;` present
  - `--accent-hover: #2B5DCC;` present
  - `--success-text: #1d5b35;` present in :root
  - `--danger-text: #7a1f1f;` present in :root
  - `#1d5b35` NOT present outside :root (only the token definition)
  - `#7a1f1f` NOT present outside :root (only the token definition)
  - `#0a84ff` NOT present anywhere
  </verify>
  <acceptance_criteria>
  - `grep "#0a84ff" chrome-extension/options.html` returns no output
  - `grep "\-\-accent: #356AE6" chrome-extension/options.html` returns exactly 1 match
  - `grep "\-\-accent-hover: #2B5DCC" chrome-extension/options.html` returns exactly 1 match
  - `grep "color: #1d5b35" chrome-extension/options.html` returns no output (only `var(--success-text)`)
  - `grep "color: #7a1f1f" chrome-extension/options.html` returns no output (only `var(--danger-text)`)
  - `.btn-primary` block contains `background: var(--accent)`
  </acceptance_criteria>
  <done>options.html uses #356AE6 as its accent; Save Settings button renders in cornflower blue; status feedback colors are tokenized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS custom property → rendered color | Client-side only; no data crosses trust boundaries in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | CSS token values | accept | Color token changes are client-side CSS; no security impact; no user data involved |
</threat_model>

<verification>
After both tasks complete:
1. `grep "#0a84ff" chrome-extension/popup.html chrome-extension/options.html` → zero matches
2. `grep "\-\-accent: #356AE6" chrome-extension/popup.html chrome-extension/options.html` → 2 matches (one per file)
3. `grep "\-\-accent-hover: #2B5DCC" chrome-extension/popup.html chrome-extension/options.html` → 2 matches
4. `grep "background: #cfd3d9" chrome-extension/popup.html` → zero matches
5. dashboard.css already has `--accent: #356AE6` at line 25 — no changes needed; verify it's still there: `grep "\-\-accent:.*#356AE6" api/app/static/dashboard.css` → 1 match
</verification>

<success_criteria>
- Both extension files share the same `--accent: #356AE6` token as the dashboard
- `#0a84ff` (iOS blue) is eliminated from all extension files
- .btn-primary in both extension files renders in cornflower blue (not near-black)
- .status-indicator default uses `var(--muted)` token
- Hardcoded feedback colors (#1d5b35, #7a1f1f) are moved to named tokens in options.html :root
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-polish/02-01-SUMMARY.md`
</output>
